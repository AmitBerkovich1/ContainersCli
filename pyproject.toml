[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ContainersCli"
version = "0.1.0"
description = "Cli for running projects in containers"
requires-python = ">=3.8"

[project.scripts]
ContainersCli = "ContainersCli.main:main"